# py3-zhihu

``` shell
python py3-spider.py -h
connect to mysql
usage: py3-parser.py [-h] {user,topic,cookie} ...

positional arguments:
  {user,topic,cookie}  commands

optional arguments:
  -h, --help           show this help message and exit
```
